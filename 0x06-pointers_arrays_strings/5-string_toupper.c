#include "main.h"
#include <string.h>
#include <stdio.h>
#include <ctype.h>
/**
 * string_toupper - Changes all lowercase lettersof a string to uppercase
 * @str: The string to modify
 * @j: loop counter
 * Return: pointer to the modified string
 */
char *string_toupper(char *str)
{
	int j = 0;

	while (str[j])
	{
		_putchar(toupper(str[j]);
		j++;
	}

	return ('\n');
}
