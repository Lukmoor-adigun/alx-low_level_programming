#include "main.h"
/**
 * mul - multiply two integers
 *
 * @a: character to multiply
 * @b: character to multiply
 *
 * Return: a x b
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}

