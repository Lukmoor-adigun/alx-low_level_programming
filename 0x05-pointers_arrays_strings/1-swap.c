#include "main.h"
/**
 * swap_int - function that swaps the value of two integers
 * @a: first parameter
 * @b: second parameter
 * return: values of a swapped with b and v.versa
 */

void swap_int(int *a, int *b)
{
	int temp - *a;
	*a = *b;
	*b = temp;
}
